{
  "name": "http_server",
  "version": "1.0.0",
  "description": "HTTP server to display MongoDB Documents",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mongodb": "^3.5.7"
  }
}
